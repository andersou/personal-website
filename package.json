{
  "name": "personal-website",
  "version": "1.0.0",
  "private": true,
  "description": "Currículo / site pessoal (HTML estático)",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "browser-sync start --server --files \"**\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.4"
  },
  "engines": {
    "node": ">=20"
  }
}
